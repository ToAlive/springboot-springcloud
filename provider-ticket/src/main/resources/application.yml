server:
  port: 8002
#给应用取个名字
spring:
  application:
    name: provider-ticket

eureka:
  instance:
    prefer-ip-address: true #注册服务的时候使用服务的ip进行注册
  #服务注册地址
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/